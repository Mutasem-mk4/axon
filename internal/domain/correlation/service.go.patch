--- internal/domain/correlation/service.go
+++ internal/domain/correlation/service.go
@@ -19,10 +19,10 @@

	compact := make([]CompactFinding, 0, len(findings))
-	representatives := make(map[string]evidence.Finding, len(findings)/4+1)
-	for _, finding := range findings {
-		ref := evidence.FindingRef{Path: finding.Source.Provider}
-		item := Compact(finding, ref)
+	representatives := make(map[string]int, len(findings)/4+1)
+	for i := range findings {
+		ref := evidence.FindingRef{Path: findings[i].Source.Provider}
+		item := Compact(findings[i], ref)
		compact = append(compact, item)
-		updateRepresentative(representatives, item, finding)
+		updateRepresentative(representatives, item, i, findings)
	}

-	return correlateCompact(compact, representatives), nil
+	return correlateCompact(compact, representatives, findings), nil
 }

-func (Service) CorrelateCompact(_ context.Context, compact []CompactFinding, representatives map[string]evidence.Finding) ([]evidence.RootCauseCluster, error) {
-	return correlateCompact(compact, representatives), nil
+func (Service) CorrelateCompact(_ context.Context, compact []CompactFinding, representatives map[string]int, originalFindings []evidence.Finding) ([]evidence.RootCauseCluster, error) {
+	return correlateCompact(compact, representatives, originalFindings), nil
 }

-func correlateCompact(compact []CompactFinding, representatives map[string]evidence.Finding) []evidence.RootCauseCluster {
+func correlateCompact(compact []CompactFinding, representatives map[string]int, originalFindings []evidence.Finding) []evidence.RootCauseCluster {
	if len(compact) == 0 {
		return nil
@@ -58,7 +58,7 @@
		cluster := &clusters[index]
		cluster.FindingIDs = append(cluster.FindingIDs, item.ID)
-		cluster.Representative = representatives[id]
+		cluster.Representative = originalFindings[representatives[id]]
	}

@@ -118,10 +118,10 @@
 }

-func updateRepresentative(representatives map[string]evidence.Finding, compact CompactFinding, finding evidence.Finding) {
+func updateRepresentative(representatives map[string]int, compact CompactFinding, index int, originalFindings []evidence.Finding) {
	if compact.CorrelationKey == "" {
		return
	}

	id := compact.CorrelationType + "|" + compact.CorrelationKey
-	current, exists := representatives[id]
-	if !exists || current.Severity.Score < compact.SeverityScore {
-		representatives[id] = finding
+	currentIndex, exists := representatives[id]
+	if !exists || originalFindings[currentIndex].Severity.Score < compact.SeverityScore {
+		representatives[id] = index
	}
 }
